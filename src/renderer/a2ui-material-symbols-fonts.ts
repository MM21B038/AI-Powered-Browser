/**
 * Self-hosted Material Symbols (Outlined, Rounded, Sharp) for A2UI v0.9 `Icon` ligatures.
 * Bundled via Fontsource so CSP `font-src 'self'` is satisfied (no Google Fonts CDN).
 */
import "@fontsource-variable/material-symbols-outlined";
import "@fontsource-variable/material-symbols-rounded";
import "@fontsource-variable/material-symbols-sharp";
